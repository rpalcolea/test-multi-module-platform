plugins {
    id 'java-platform'
}

dependencies {
    constraints {
        api project(":clienta")
        api project(":clientb")
    }
}

publishing {
    publications {
        myPlatform(MavenPublication) {
            from components.javaPlatform
        }
    }
}
